{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Cortex Debug",
            "cwd": "${workspaceFolder}",
            "executable": "./build/Debug/H7cpu.elf",
            "request": "launch",
            "type": "cortex-debug",
            "runToEntryPoint": "main",
            "servertype": "jlink",
            "device": "STM32H743XI",
            "interface": "swd",
            "gdbPath": "gdb-multiarch",  // 添加这一行
            "svdFile": "${workspaceFolder}/STM32H743.svd"
        }
    ]
}